<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>:( SYSTEM_CRASH_HERO_DETECTED</title>
    <style>
        /* Основной синий фон как в Windows */
        body {
            background-color: #0078d7; 
            color: #ffffff;
            font-family: "Segoe UI", "Segoe UI Symbol", "Helvetica Neue", "Arial", sans-serif;
            display: flex;
            align-items: center;
            justify-content: center;
            height: 100vh;
            margin: 0;
            overflow: hidden;
            cursor: default;
            user-select: none;
        }

        .container {
            width: 80%;
            max-width: 900px;
        }

        .sad-face {
            font-size: 120px;
            margin-bottom: 20px;
        }

        h1 {
            font-size: 32px;
            font-weight: 300;
            line-height: 1.4;
            margin-bottom: 30px;
        }

        .details {
            display: flex;
            align-items: flex-start;
            gap: 30px;
            margin-top: 40px;
        }

        /* Наша гифка с Наруто */
        .naruto-gif {
            width:
